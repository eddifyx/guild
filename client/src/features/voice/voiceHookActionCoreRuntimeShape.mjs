export function buildUseVoiceHookActionCoreRuntime({
  resetScreenShareAdaptation,
  maybeAdaptScreenShareProfile,
  ensureSecureMediaReady,
  syncVoiceE2EState,
  getUntrustedVoiceParticipants,
  buildVoiceTrustError,
  syncVoiceParticipants,
  clearVoiceHealthProbe,
  switchLiveCaptureModeInPlace,
  reconfigureLiveCapture,
  scheduleVoiceHealthProbe,
  cleanupRemoteProducer,
  createSendTransport,
  createRecvTransport,
  consumeProducer,
  applyLiveCaptureToProducer,
  resumeVoiceMediaAfterKeyUpdate,
} = {}) {
  return {
    resetScreenShareAdaptation,
    maybeAdaptScreenShareProfile,
    ensureSecureMediaReady,
    syncVoiceE2EState,
    getUntrustedVoiceParticipants,
    buildVoiceTrustError,
    syncVoiceParticipants,
    clearVoiceHealthProbe,
    switchLiveCaptureModeInPlace,
    reconfigureLiveCapture,
    scheduleVoiceHealthProbe,
    cleanupRemoteProducer,
    createSendTransport,
    createRecvTransport,
    consumeProducer,
    applyLiveCaptureToProducer,
    resumeVoiceMediaAfterKeyUpdate,
  };
}
